#include <main.h>
/**
 * _isupper - check if parameter is an uppercase character
 * @c: input character.
 * Return: l if is an uppercase character, 0 in other case.
 */
int _isupper(int c)
{
	if (c >= 60 && c <= 90|)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
